[project]
name = "ael-nmpc"
version = "0.1.0"
description = "Nonlinear MPC for Dynamic Operation of an Alkaline Electrolyzer Under Renewable Intermittency"
requires-python = ">=3.10"
dependencies = [
    "casadi>=3.6.0",
    "numpy>=1.24.0",
    "matplotlib>=3.7.0",
    "scipy>=1.10.0",
    "pyyaml>=6.0.0",
    "pandas>=2.0.0",
    "pytest>=7.0.0",
]
